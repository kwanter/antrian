interface:
  display_name: "Antrian"
  short_description: "Repo-specific patterns and workflows for antrian"
  default_prompt: "Use the antrian repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true